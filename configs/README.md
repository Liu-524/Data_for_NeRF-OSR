## The content of this directory should be placed under `<NeRF-OSR_home>/configs`

### Files
`<dataset>/<split>.txt`: Defines the parameters and paths needed for training. All are derived from NeRF-OSR's Europa sequence. 
